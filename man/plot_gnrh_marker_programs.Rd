% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_gnrh_marker_programs}
\alias{plot_gnrh_marker_programs}
\title{Plot GnRH Marker Program Results}
\usage{
plot_gnrh_marker_programs(
  programs,
  table = c("summary", "high_confidence", "candidate_table"),
  type = c("bar", "dot", "tile"),
  min_genes = 1,
  style = "classic",
  mode = "light",
  txtsize = 12,
  x.ang = 45
)
}
\arguments{
\item{programs}{Output list returned by \code{\link{gnrh_marker_programs}}.}

\item{table}{Character. Which result table to visualize. One of
\code{"summary"}, \code{"high_confidence"}, or
\code{"candidate_table"}. Default is \code{"summary"}.}

\item{type}{Character. Plot type to generate. One of \code{"bar"},
\code{"dot"}, or \code{"tile"}. For gene-level tables
(\code{"high_confidence"} or \code{"candidate_table"}), this argument is
ignored and a gene-level dotplot is returned.}

\item{min_genes}{Integer. Minimum number of genes required for a
program/dataset combination to be displayed when
\code{table = "summary"}. Default is \code{1}.}

\item{style}{Character. Theme style passed to \code{\link{plot_theme}}.
One of \code{"classic"}, \code{"minimal"}, \code{"bw"}, \code{"test"},
\code{"void"}, \code{"dirty"}, or \code{"gray"}. Default is
\code{"classic"}.}

\item{mode}{Character. Theme mode passed to \code{\link{plot_theme}}.
One of \code{"light"} or \code{"dark"}. Default is \code{"light"}.}

\item{txtsize}{Numeric. Base text size passed to \code{\link{plot_theme}}.
Default is \code{12}.}

\item{x.ang}{Numeric. Rotation angle for x-axis labels. Default is
\code{45}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Visualize candidate GnRH marker programs generated by
\code{\link{gnrh_marker_programs}}.
}
\details{
This function generates publication-ready plots summarizing candidate
GnRH-associated marker genes, developmental programs, confidence levels,
and dataset-specific marker signatures.

When \code{table = "summary"}, three plot types are available:

\itemize{
\item \code{type = "bar"}: barplot of candidate marker counts per
developmental program, faceted by dataset.
\item \code{type = "dot"}: dotplot showing developmental program
enrichment across datasets.
\item \code{type = "tile"}: heatmap-like tile plot showing the number of
candidate genes per developmental program and dataset.
}

When \code{table = "high_confidence"} or
\code{table = "candidate_table"}, the function returns a gene-level
dotplot with genes on the y-axis, developmental programs on the x-axis,
point color representing confidence level, and point size representing
specificity score.

The plotting theme is controlled by \code{\link{plot_theme}}, the internal
GnRHcell theme system.
}
\examples{
\dontrun{
programs <- gnrh_marker_programs(
  files = files,
  results = overlap,
  outdir = outdir
)

# Summary barplot
p1 <- plot_gnrh_marker_programs(
  programs,
  table = "summary",
  type = "bar"
)

# Summary dotplot
p2 <- plot_gnrh_marker_programs(
  programs,
  table = "summary",
  type = "dot"
)

# Summary tile plot
p3 <- plot_gnrh_marker_programs(
  programs,
  table = "summary",
  type = "tile"
)

# High-confidence gene-level plot
p4 <- plot_gnrh_marker_programs(
  programs,
  table = "high_confidence"
)

# Dark mode
p5 <- plot_gnrh_marker_programs(
  programs,
  table = "summary",
  type = "dot",
  mode = "dark"
)
}

}
\seealso{
\code{\link{gnrh_marker_programs}},
\code{\link{gnrh_stage_modules}},
\code{\link{gene_upset}},
\code{\link{plot_theme}}
}
