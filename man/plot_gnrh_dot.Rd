% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plot_gnrh_dot}
\alias{plot_gnrh_dot}
\title{Create an enhanced Seurat dot plot}
\usage{
plot_gnrh_dot(
  object,
  features,
  group.by = "seurat_clusters",
  th.cols = "RdYlBu",
  rev.th.cols = TRUE,
  dot.scale = 6,
  x.ang = 45,
  vjust.x = NULL,
  hjust.x = NULL,
  flip = FALSE,
  txtsize = 12,
  title = NULL,
  leg.size = 10,
  leg.ttl.size = 10,
  leg.pos = "right",
  leg.just = "bottom",
  leg.hjust = FALSE,
  x.axis.pos = "bottom",
  style = "classic",
  x.face = FALSE,
  y.face = FALSE,
  x.ttl = FALSE,
  y.ttl = FALSE,
  dot.outline = FALSE,
  ...
)
}
\arguments{
\item{object}{A Seurat object.}

\item{features}{Character vector or named list of features to plot.}

\item{group.by}{Metadata column used to group cells.
Default is \code{"seurat_clusters"}.}

\item{th.cols}{RColorBrewer palette name used for the expression gradient.
Default is \code{"RdYlBu"}.}

\item{rev.th.cols}{Logical; reverse the color gradient.
Default is \code{TRUE}.}

\item{dot.scale}{Numeric dot-size scaling factor passed to
\code{Seurat::DotPlot()}. Default is \code{4}.}

\item{x.ang}{Angle of x-axis labels in degrees. Default is \code{90}.}

\item{vjust.x, hjust.x}{Vertical and horizontal justification for x-axis labels.}

\item{flip}{Logical; flip x and y axes using \code{ggplot2::coord_flip()}.
Default is \code{FALSE}.}

\item{txtsize}{Base text size.
Default is \code{12}.}

\item{title}{Optional plot title.}

\item{leg.size}{Legend text size. Default is \code{10}.}

\item{leg.ttl.size}{Legend title size. Default is \code{10}.}

\item{leg.pos}{Legend position. One of \code{"right"}, \code{"left"},
\code{"top"}, \code{"bottom"}, or \code{NULL}.
Default is \code{"right"}.}

\item{leg.just}{Legend justification. Default is \code{"bottom"}.}

\item{leg.hjust}{Logical; reserved for legend layout customization.
Default is \code{FALSE}.}

\item{x.axis.pos}{Position of the x-axis. Default is \code{"bottom"}.}

\item{style}{Theme style.
Default is \code{"classic"}.}

\item{x.face, y.face}{Logical; italicize x- or y-axis labels.
Default is \code{FALSE}.}

\item{x.ttl, y.ttl}{Logical; show x- or y-axis titles.
Default is \code{FALSE}.}

\item{dot.outline}{Logical; draw outlines around dots.
Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{Seurat::DotPlot()} and
the internal gnrhcell theme.}
}
\value{
A \code{ggplot2} object.
}
\description{
Generates a customizable dot plot for gene expression patterns across
clusters or metadata-defined groups. This function wraps
\code{Seurat::DotPlot()} and adds improved color control, optional dot
outlines, flexible axis formatting, legend customization, and gnrhcell
theme support.
}
\details{
Dot color represents average expression and dot size represents the
percentage of cells expressing each feature, following the standard
\code{Seurat::DotPlot()} convention.

When \code{dot.outline = TRUE}, dots are drawn with a light outline using
shape 21. This can improve readability in publication figures.
}
\examples{
\dontrun{
celldot(pbmc, features = c("MS4A1", "CD3D"))

celldot(
  pbmc,
  features = c("MS4A1", "CD14"),
  th.cols = "Blues",
  dot.outline = TRUE,
  flip = TRUE
)
}

}
