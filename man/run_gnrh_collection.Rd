% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{run_gnrh_collection}
\alias{run_gnrh_collection}
\title{Run GnRHcell across multiple datasets}
\usage{
run_gnrh_collection(
  datasets,
  output_dir,
  run_markers = TRUE,
  run_comparisons = TRUE,
  run_programs = TRUE,
  clean_objects = TRUE,
  save_objects = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{datasets}{A data frame or tibble containing the columns \code{id}, \code{label},
\code{species}, \code{file}, \code{split_by}, and \code{reduction}.}

\item{output_dir}{Character scalar giving the root output directory.}

\item{run_markers}{Logical. Whether to identify GnRH markers for each
dataset. Default is \code{TRUE}.}

\item{run_comparisons}{Logical. Whether to perform cross-dataset
comparisons after all datasets have been processed. Default is \code{TRUE}.}

\item{run_programs}{Logical. Whether to identify conserved marker programs
during cross-dataset comparisons. Default is \code{TRUE}.}

\item{clean_objects}{Logical. Whether processed Seurat objects should be
removed from the returned dataset-level results to reduce memory usage.
Default is \code{TRUE}.}

\item{save_objects}{Logical. Whether processed Seurat objects should be
saved to disk. Default is \code{FALSE}.}

\item{verbose}{Logical. Whether to print progress messages. Default is
\code{TRUE}.}
}
\value{
An object of class \code{"gnrh_collection"} containing:
\describe{
\item{datasets}{The dataset configuration table used for the analysis.}
\item{results}{Named list of dataset-level GnRHcell results.}
\item{comparisons}{Cross-dataset comparison results, or \code{NULL}.}
\item{output_dir}{Normalized output directory.}
}
}
\description{
Applies the GnRHcell workflow to a collection of Seurat datasets and
optionally performs cross-dataset marker comparisons and conserved marker
program analysis.
}
\details{
Each row of \code{datasets} represents one dataset. Dataset files are loaded
sequentially to limit memory usage.
}
\examples{
\dontrun{
datasets <- data.frame(
  id = c("human_hpsc", "mouse_hypomap"),
  label = c("Human hPSC GnRH", "Mouse HypoMap"),
  species = c("Human", "Mouse"),
  file = c("human_hpsc.rds", "mouse_hypomap.rds"),
  split_by = c("orig.ident", "orig.ident"),
  reduction = c("umap", "umap")
)

datasets <- prepare_gnrh_datasets(datasets)

results <- run_gnrh_collection(
  datasets = datasets,
  output_dir = "gnrh_results"
)
}

}
\seealso{
\code{\link[=prepare_gnrh_datasets]{prepare_gnrh_datasets()}}, \code{\link[=run_gnrh_dataset]{run_gnrh_dataset()}},
\code{\link[=compare_gnrh_datasets]{compare_gnrh_datasets()}}
}
