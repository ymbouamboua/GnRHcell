% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{cellviolin}
\alias{cellviolin}
\title{Create violin plots for Seurat features}
\usage{
cellviolin(
  obj,
  features,
  ncol = NULL,
  shared.y = FALSE,
  ttl.pos = c("left", "center", "right"),
  group.by = "seurat_clusters",
  split.by = NULL,
  stack = FALSE,
  assay = "RNA",
  slot = "data",
  log = FALSE,
  cols = NULL,
  med = FALSE,
  med.size = 1,
  pt.size = 0,
  border.size = 0.1,
  style = "classic",
  leg.pos = "none",
  x.ang = 45,
  title = NULL,
  rm.subttl = FALSE,
  flip = FALSE,
  auto.resize = TRUE,
  ylab.global = NULL,
  xlab.global = NULL,
  add.stats = FALSE,
  show.pval = FALSE,
  pairwise = FALSE,
  pval.label = "p.signif",
  p.display = c("stars", "value", "none"),
  txtsize = 10,
  subttl.size = 10,
  ...
)
}
\arguments{
\item{obj}{A Seurat object.}

\item{features}{Character vector of genes or metadata columns to plot.}

\item{ncol}{Number of columns in the patchwork layout. If \code{NULL},
selected automatically from the number of features.}

\item{shared.y}{Logical; use the same y-axis range for all features.
Default is \code{FALSE}.}

\item{ttl.pos}{Subplot title position. One of \code{"left"},
\code{"center"}, or \code{"right"}.}

\item{group.by}{Metadata column used to group cells.
Default is \code{"seurat_clusters"}.}

\item{split.by}{Optional metadata column used to split violin plots.}

\item{stack}{Logical; arrange plots in a single column.
Default is \code{FALSE}.}

\item{assay}{Assay used to retrieve gene expression.
Default is \code{"RNA"}.}

\item{slot}{Assay slot used for expression values. One of \code{"data"},
\code{"counts"}, or \code{"scale.data"}. Default is \code{"data"}.}

\item{log}{Logical; if \code{TRUE} and \code{slot = "counts"}, use
log-transformed counts. Default is \code{FALSE}.}

\item{cols}{Optional named color vector for groups.}

\item{med}{Logical; overlay median points on violin plots.
Default is \code{FALSE}.}

\item{med.size}{Size of median points. Default is \code{1}.}

\item{pt.size}{Size of jittered cells. Use \code{0} to hide points.
Default is \code{0}.}

\item{border.size}{Violin outline linewidth. Default is \code{0.1}.}

\item{style}{Theme style passed to \code{\link{plot_theme}}.
Default is \code{"classic"}.}

\item{leg.pos}{Legend position. Default is \code{"none"}.}

\item{x.ang}{X-axis text angle. Default is \code{45}.}

\item{title}{Optional global title for the combined plot.}

\item{rm.subttl}{Logical; remove individual feature titles.
Default is \code{FALSE}.}

\item{flip}{Logical; flip x and y axes. Default is \code{FALSE}.}

\item{auto.resize}{Logical; add dynamic width and height attributes to
the returned object. Default is \code{TRUE}.}

\item{ylab.global}{Optional global y-axis label. If \code{NULL}, inferred
from \code{slot}.}

\item{xlab.global}{Optional global x-axis label. Default is blank.}

\item{add.stats}{Logical; compute statistical comparisons.
Default is \code{FALSE}.}

\item{show.pval}{Logical; display p-values or significance labels.
Default is \code{FALSE}.}

\item{pairwise}{Logical; perform pairwise Wilcoxon tests between groups.
If \code{FALSE}, a global Kruskal-Wallis test is used.
Default is \code{FALSE}.}

\item{pval.label}{Label type passed to statistical annotation utilities.
Default is \code{"p.signif"}.}

\item{p.display}{How to display statistical results. One of
\code{"stars"}, \code{"value"}, or \code{"none"}.
Default is \code{"stars"}.}

\item{txtsize}{Base text size. Default is \code{10}.}

\item{subttl.size}{Feature subplot title size. Default is \code{10}.}

\item{...}{Additional arguments passed to \code{Seurat::VlnPlot()} and
\code{\link{plot_theme}}.}
}
\value{
A patchwork/cowplot object containing the violin plots.
}
\description{
Generates publication-ready violin plots for one or more genes or metadata
features from a Seurat object. This function wraps \code{Seurat::VlnPlot()}
and adds shared y-axis scaling, median markers, optional statistical tests,
flexible layout, global axis labels, and GnRHcell theme support.
}
\details{
Features can be either assay genes or metadata columns. Missing features are
silently ignored; an error is raised if none of the requested features are
found.

When \code{add.stats = TRUE} and \code{show.pval = TRUE}, statistical
annotations are added if at least two groups are available. Pairwise tests
use Wilcoxon rank-sum tests, while the global comparison uses a
Kruskal-Wallis test.
}
\examples{
\dontrun{
cellviolin(
  obj,
  features = c("GNRH1", "ISL1", "DLX5"),
  group.by = "gnrh_stage",
  pt.size = 0.1,
  ncol = 3
)

cellviolin(
  obj,
  features = c("GNRH1", "ISL1"),
  group.by = "gnrh_status",
  add.stats = TRUE,
  show.pval = TRUE,
  pairwise = TRUE
)
}

}
