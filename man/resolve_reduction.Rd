% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{resolve_reduction}
\alias{resolve_reduction}
\title{Resolve an available dimensional reduction}
\usage{
resolve_reduction(
  object,
  reduction = NULL,
  fallback = c("umap", "umap_scvi", "umap.harmony", "umap.rpca", "pca")
)
}
\arguments{
\item{object}{A Seurat object containing dimensional reductions.}

\item{reduction}{Optional character string specifying the preferred
dimensional reduction. If \code{NULL} or unavailable, a fallback is selected.}

\item{fallback}{Character vector defining the preferred fallback order.
Defaults to \code{"umap"}, \code{"umap_scvi"}, \code{"umap.harmony"}, \code{"umap.rpca"},
and \code{"pca"}.}
}
\value{
A character string giving the name of the selected dimensional
reduction.
}
\description{
Selects a dimensional reduction from a Seurat object. If the requested
reduction is unavailable, the function searches a predefined fallback list,
then any reduction containing \code{"umap"}, and finally returns the first
available reduction.
}
\details{
Reduction selection follows this priority:
\enumerate{
\item The explicitly requested \code{reduction}, when available.
\item The first available reduction listed in \code{fallback}.
\item The first available reduction whose name contains \code{"umap"}.
\item The first dimensional reduction stored in the object.
}
An error is raised when the object contains no dimensional reductions.
}
\examples{
\dontrun{
resolve_reduction(object)
resolve_reduction(object, "umap_scvi")
resolve_reduction(object, fallback = c("umap", "pca"))
}
}
\keyword{internal}
