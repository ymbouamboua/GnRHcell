% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{cellmap}
\alias{cellmap}
\title{Visualize cells on a Seurat embedding}
\usage{
cellmap(
  object,
  group.by = NULL,
  reduction = "umap",
  dims = c(1, 2),
  shuffle = FALSE,
  raster = NULL,
  stroke.size = NULL,
  raster.dpi = c(2048, 2048),
  alpha = 1,
  repel = FALSE,
  n.cells = TRUE,
  label = FALSE,
  label.size = 4,
  label.face = "plain",
  cols = NULL,
  figplot = FALSE,
  axes = TRUE,
  plot.ttl = NULL,
  legend = TRUE,
  leg.ttl = NULL,
  leg.ttl.size = txtsize,
  item.size = 4,
  leg.pos = "right",
  leg.just = "center",
  leg.dir = "vertical",
  leg.size = 10,
  leg.ncol = NULL,
  item.border = TRUE,
  txtsize = 12,
  pt.size = NULL,
  dark = FALSE,
  total.cells = FALSE,
  threeD = FALSE,
  style = "test",
  facet.bg = FALSE,
  ...
)
}
\arguments{
\item{object}{A Seurat object.}

\item{group.by}{Metadata column used to color cells. If \code{NULL},
active identities are used. A character vector of multiple metadata columns
can be supplied to generate a patchwork of plots.}

\item{reduction}{Dimensional reduction to plot. Default is \code{"umap"}.}

\item{dims}{Numeric vector specifying dimensions to plot. Use two values for
2D plots or three values for 3D plots. Default is \code{c(1, 2)}.}

\item{shuffle}{Logical; randomly shuffle plotting order of cells.
Default is \code{FALSE}.}

\item{raster}{Logical; rasterize points for large datasets. If \code{NULL},
rasterization is enabled automatically for objects with more than 100,000
cells. Default is \code{NULL}.}

\item{stroke.size}{Optional point stroke size passed to \code{Seurat::DimPlot()}.}

\item{raster.dpi}{Numeric vector of length 2 specifying rasterization
resolution. Default is \code{c(2048, 2048)}.}

\item{alpha}{Numeric point transparency. Default is \code{1}.}

\item{repel}{Logical; repel cluster labels when labels are drawn.
Default is \code{FALSE}.}

\item{n.cells}{Logical; append the number of cells per group to legend labels.
Default is \code{TRUE}.}

\item{label}{Logical; add cluster labels to the embedding.
Default is \code{FALSE}.}

\item{label.size}{Numeric label text size. Default is \code{4}.}

\item{label.face}{Font face for cluster labels. Default is \code{"plain"}.}

\item{cols}{Optional named color vector. Missing groups are assigned
\code{"gray70"}. If unnamed, colors are matched to group levels in order.}

\item{figplot}{Logical; generate a minimal figure-style plot with compact
arrow axes. Default is \code{FALSE}.}

\item{axes}{Logical; show embedding axes. Default is \code{TRUE}.}

\item{plot.ttl}{Optional plot title.}

\item{legend}{Logical; show legend. Default is \code{TRUE}.}

\item{leg.ttl}{Optional legend title. If \code{NULL}, \code{group.by} is used.}

\item{leg.ttl.size}{Numeric legend title size. Default is \code{txtsize}.}

\item{item.size}{Numeric legend item size. Default is \code{4}.}

\item{leg.pos}{Legend position passed to \code{\link{plot_theme}}.
Default is \code{"right"}.}

\item{leg.just}{Legend justification. Default is \code{"center"}.}

\item{leg.dir}{Legend direction. Default is \code{"vertical"}.}

\item{leg.size}{Numeric legend text size. Default is \code{10}.}

\item{leg.ncol}{Number of legend columns. If \code{NULL}, this is selected
automatically based on the number of groups.}

\item{item.border}{Logical; draw borders around legend keys.
Default is \code{TRUE}.}

\item{txtsize}{Base text size. Default is \code{12}.}

\item{pt.size}{Numeric point size. If \code{NULL}, a point size is chosen
automatically based on cell number and rasterization mode.}

\item{dark}{Logical; apply a dark theme. Default is \code{FALSE}.}

\item{total.cells}{Logical; append total cell number to the plot title.
Default is \code{FALSE}.}

\item{threeD}{Logical; generate an interactive 3D plot using \pkg{plotly}.
Default is \code{FALSE}.}

\item{style}{Theme style passed to \code{\link{plot_theme}}.
Default is \code{"test"}.}

\item{facet.bg}{Logical; show facet background in the applied theme.
Default is \code{FALSE}.}

\item{...}{Additional arguments passed to \code{Seurat::DimPlot()} and
\code{\link{plot_theme}}.}
}
\value{
A \code{ggplot2} object for 2D plots, a patchwork object when
multiple \code{group.by} variables are supplied, or a plotly object for
3D plots.
}
\description{
Creates a customizable two-dimensional or three-dimensional embedding plot
for Seurat objects. This function wraps \code{Seurat::DimPlot()} and adds
improved color handling, automatic rasterization, custom legends, optional
cluster labels, figure-panel styling, dark mode, and support for plotting
multiple metadata variables.
}
\details{
Cell identities are prepared internally to preserve factor-level order and
handle missing values as \code{"Unknown"}. Colors are strictly matched to
the groups present in the plot.

If \code{figplot = TRUE}, a minimal publication-panel style is used and a
compact axis-arrow inset is added. If \code{dark = TRUE}, a dark theme is
applied after the main theme.
}
\examples{
\dontrun{
cellmap(obj, group.by = "seurat_clusters")

cellmap(
  obj,
  group.by = c("gnrh_status", "gnrh_stage"),
  reduction = "umap",
  label = TRUE
)

cellmap(
  obj,
  group.by = "gnrh_stage",
  dark = TRUE,
  legend = TRUE
)
}

}
